# binary-search-tree

This is a simple implementation of a **Binary Search Tree (BST)** in C.  
It takes 10 integer inputs from the user, adds them to the BST, and then displays the elements using **in-order traversal** (sorted order).

## 🧠 Features
- Dynamic memory allocation using `malloc`
- Duplicate values are not inserted
- In-order traversal (Left → Root → Right)

## 📁 File Structure